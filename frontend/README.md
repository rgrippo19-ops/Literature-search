# Frontend

## Local use
Open `index.html` with Live Server, or deploy the whole `frontend/` folder to Netlify.

Set your backend URL in `config.js`:

```js
window.APP_CONFIG = {
  apiBaseUrl: "http://127.0.0.1:8000"
};
```
